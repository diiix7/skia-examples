import { StyleSheet } from "react-native";
const AnimatedCircleStyle = StyleSheet.create({
  view: {
    flex: 1,
  },
  canvas: {
    width: 390,
    height: 200,
  },
});
export default AnimatedCircleStyle;
